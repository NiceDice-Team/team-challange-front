services:
  frontend:
    build: .
    container_name: frontend_dev
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_BACKEND_URL: ${NEXT_PUBLIC_BACKEND_URL:-http://backend:8000/api/}
    command: npm run dev
