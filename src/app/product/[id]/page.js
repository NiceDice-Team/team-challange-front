import ProductPage from "../../../components/Product_page/Single_product_page";

export default function ProductDetailPage({ params }) {
  // You can use params.id to fetch the specific product data
  return <ProductPage />;
}
